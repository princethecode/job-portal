<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jobportal">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Contact permissions -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Storage permissions with proper SDK targeting -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Wake lock for background operations -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Hardware features (optional) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    
    <!-- Screen support -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:name=".JobPortalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobPortal"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <service
            android:name=".services.ContactSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ResetPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ui.jobs.AvailableJobsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.applications.AppliedJobsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.notifications.NotificationsActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ChangePasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.update.UpdateActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal.NoActionBar" />

        <!-- Recruiter Activities -->
        <activity
            android:name=".auth.RecruiterLoginActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal.NoActionBar" />

        <activity
            android:name=".auth.RecruiterRegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal.NoActionBar" />

        <activity
            android:name=".recruiter.RecruiterMainActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal.NoActionBar" />

        <activity
            android:name=".recruiter.RecruiterProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal" />

        <activity
            android:name=".recruiter.JobPostingActivity"
            android:exported="false"
            android:theme="@style/Theme.JobPortal"
            android:parentActivityName=".recruiter.RecruiterMainActivity" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.JobPortalFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default notification icon and color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary" />

    </application>
</manifest> 